name : Check-Out Code
on: 
 push:
   branches: 
    - main
jobs:
 test:
  runs-on: ubuntu-latest
  steps:
   - name: checkout code
     uses: actions/checkout@v3
   - name: show files
     run : ls -lrtha
